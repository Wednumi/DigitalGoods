@inject NavigationManager NavigationManager

<div @onclick=CallSignOutPage>
    @ChildContent
</div>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    private void CallSignOutPage(){
        NavigationManager.NavigateTo("/account/signout", true);
    }
}
